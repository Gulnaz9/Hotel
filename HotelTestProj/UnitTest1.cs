namespace HotelTestProj
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {
        }

        [Test]
        public void Timer_Tick_Should_Update_DateTimeLbl()  //не разобралась до конца
        { /*
            // Arrange
            var form = new Form();
            var sender = new object();
            var e = new EventArgs();

            // Act
            form.Timer_Tick(sender, e);

            // Assert
            Assert.AreEqual(DateTimeLbl.Text, DateTime.Now.ToString("dd.MM.yyyy HH:mm:ss"));
            */
        }
    }
}